import React from 'react';

const Reset = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '50px' }}>
            <h1>Lets change the password</h1>
        </div>
    );
};

export default Reset;